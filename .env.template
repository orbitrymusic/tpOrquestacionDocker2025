
--- 1. CONFIGURACIÓN GENERAL DEL SERVICIO ---

---------------------------------------------

NODE_ENV=development
PORT=3000
LOG_LEVEL=info # Nivel de severidad para los logs internos (debug, info, warn, error, fatal).

--- 2. BASE DE DATOS Y SEGURIDAD ---

------------------------------------

MONGODB_URI= # URL de conexión a la base de datos MongoDB. Ejemplo: mongodb://localhost:27017/auth-service-db

Parámetros para la generación y validación de JSON Web Tokens (JWT)

JWT_SECRET= # Clave secreta fuerte (mínimo 32 caracteres) para firmar tokens.
JWT_EXPIRATION= # Tiempo de expiración del token. Ejemplos: 1h, 7d, 240000ms.

=======================================================

--- 3. INTEGRACIONES CON SERVICIOS CTI ---

=======================================================

3.1. Microservicio CORE (Listado de Preinscripción, Sincronización)

--------------------------------------------------------------------

CORE_SERVICE_URL= # URL base del Microservicio CORE (e.g., http://core-service:4000)
CORE_API_KEY= # Clave secreta (X-API-KEY) compartida con CORE para autenticar las peticiones.

3.2. Microservicio NOTIFICACIONES (Envío de Emails)

---------------------------------------------------

NOTIFICATION_SERVICE_URL= # URL base del Microservicio de Notificaciones (e.g., http://notifications-service:5000)

3.3. MÓDULO LOGGING (ds-appi-logging-producer) - Configuración AMQP

Importante: Todas estas variables son obligatorias para el productor de logs.

-----------------------------------------------------------------------------

AMQP_USER=logger
AMQP_PASS=KafkaAlsk32
AMQP_HOST=149.50.150.224 # Host/IP del Message Broker (RabbitMQ)
AMQP_PORT=5672
AMQP_VHOST=/
AMQP_EXCHANGE=logs.exchange
AMQP_ROUTING_KEY=logs.info
MODULE_NAME=auth-service # <-- DEBE IR EL NOMBRE ÚNICO DE ESTE MÓDULO (e.g., auth-service)
VALIDATE=true # Flag de validación (dejar siempre en true según especificación CTI)